{
  "name": "serverless-task-api",
  "version": "1.0.0",
  "description": "serverless-task-api",
  "main": "src/handler.js",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "scripts": {
    "test": "jest",
    "deploy": "serverless deploy"
  }
}
